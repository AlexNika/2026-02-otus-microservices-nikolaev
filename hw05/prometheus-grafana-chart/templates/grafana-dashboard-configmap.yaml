apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "prometheus-grafana-chart.fullname" . }}-dashboard
  labels:
    {{ include "prometheus-grafana-chart.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  hw05-application-dashboard.json: |-
    {{ .Files.Get "dashboards/hw05-application-dashboard.json" | nindent 4 }}
